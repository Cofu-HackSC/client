class User {
  final String id, name, bio, photoURL, address, location;
  final bool emailContact;
  final String contact;

  const User({
    this.id,
    this.name,
    this.bio,
    this.photoURL,
    this.address,
    this.location,
    this.emailContact,
    this.contact,
  });
}
